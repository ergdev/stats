var chai = require('chai');
var expect = chai.expect;
var stats = require('../lib/stats');

describe('Coefficient of variation', function() {
    it('should return the coefficient of variation of an array of ints', function() {
    });
});

